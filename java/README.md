# Java Hello World

This is a minimal Java application that prints "Hello World" to the terminal.

To compile and run from the `java` directory:

```sh
javac HelloWorld.java
java HelloWorld
```

Or from the repository root using the included script:

```sh
./run-java.sh
```
